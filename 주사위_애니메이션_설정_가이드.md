# 주사위 애니메이션 시스템 설정 가이드

Library of Ruina 스타일의 주사위 애니메이션을 구현한 가이드입니다.

## 📋 구현된 기능

✅ **주사위 롤링 애니메이션**
- 회전하면서 랜덤 숫자가 변경되는 효과
- 확대/축소 애니메이션
- 최종 결과 강조 펄스 효과

✅ **승패 애니메이션**
- 승리: 노란색 플래시 + 확대
- 패배: 회색 플래시 + 축소
- 무승부: 좌우 흔들림

✅ **타입별 색상**
- Attack (공격): 빨간색
- Defense (방어): 파란색

✅ **사운드 효과**
- 주사위 굴리는 사운드
- 결과 표시 사운드
- 충돌 사운드
- 데미지 사운드

---

## 🎨 Unity 설정 방법

### 1단계: DiceVisual 프리팹 만들기

1. **Hierarchy에서 UI 오브젝트 생성:**
   ```
   우클릭 → UI → Panel (이름: DiceVisual)
   ```

2. **DiceVisual 구조:**
   ```
   DiceVisual (Panel)
   ├─ Background (Image) - 배경색 (공격/방어)
   ├─ ValueText (TextMeshProUGUI) - 주사위 값 표시
   └─ TypeIcon (Image, 선택사항) - 공격/방어 아이콘
   ```

3. **DiceVisual 스크립트 추가:**
   - DiceVisual 오브젝트에 `DiceVisual.cs` 컴포넌트 추가
   - AudioSource 컴포넌트 자동 추가됨

4. **Inspector 설정:**
   - **UI 요소:**
     - Dice Background: Background Image 드래그
     - Dice Value Text: ValueText 드래그
     - Dice Type Icon: TypeIcon 드래그 (선택사항)

   - **색상 설정:**
     - Attack Color: 빨간색 (R:255, G:76, B:76)
     - Defense Color: 파란색 (R:76, G:153, B:255)

   - **애니메이션 설정:**
     - Roll Duration: 0.8초 (주사위 굴리는 시간)
     - Scale Up Amount: 1.3 (확대 크기)
     - Rotation Speed: 720 (회전 속도)

   - **사운드:**
     - Dice Roll Sound: 주사위 굴리는 효과음
     - Dice Result Sound: 결과 표시 효과음

5. **프리팹으로 저장:**
   - `Assets/Prefabs/UI/` 폴더에 드래그하여 프리팹 생성

---

### 2단계: DiceAnimationManager 설정

1. **Hierarchy에 빈 오브젝트 생성:**
   ```
   우클릭 → Create Empty (이름: DiceAnimationManager)
   ```

2. **DiceAnimationManager 스크립트 추가:**
   - `DiceAnimationManager.cs` 컴포넌트 추가

3. **주사위 컨테이너 생성:**
   ```
   Canvas (BattleCanvas)
   ├─ PlayerDiceContainer (빈 Panel)
   ├─ BossDiceContainer (빈 Panel)
   └─ CenterClashArea (빈 Panel, 선택사항)
   ```

4. **Inspector 설정:**
   - **UI 위치:**
     - Player Dice Container: PlayerDiceContainer 드래그
     - Boss Dice Container: BossDiceContainer 드래그
     - Center Clash Area: CenterClashArea 드래그 (선택사항)

   - **프리팹:**
     - Dice Visual Prefab: 1단계에서 만든 DiceVisual 프리팹

   - **애니메이션 타이밍:**
     - Delay Between Dice: 0.5초
     - Delay After Roll: 0.8초
     - Delay After Clash: 1.2초

   - **사운드:**
     - Clash Sound: 충돌 효과음
     - Damage Sound: 데미지 효과음

---

### 3단계: BattleController 연결

1. **BattleController 오브젝트 찾기:**
   - Hierarchy에서 BattleController를 가진 오브젝트 선택

2. **Inspector에서 연결:**
   - **주사위 애니메이션:**
     - Dice Animation Manager: 2단계에서 만든 DiceAnimationManager 드래그

---

## 📐 UI 레이아웃 추천

### 화면 배치:
```
┌─────────────────────────────────────┐
│  [보스 체력바]                       │
│                                      │
│  [Boss Dice]  [Boss Dice]  [Boss Dice]  ← BossDiceContainer
│                                      │
│         [중앙 전투 영역]              │
│                                      │
│  [Player Dice] [Player Dice] [Player Dice]  ← PlayerDiceContainer
│                                      │
│  [플레이어 체력바]                    │
└─────────────────────────────────────┘
```

### DiceVisual 크기:
- Width: 80~100
- Height: 80~100
- Spacing: 10~20 (컨테이너의 Horizontal Layout Group 사용)

### 컨테이너 설정:
```
PlayerDiceContainer / BossDiceContainer:
- Add Component → Horizontal Layout Group
  - Spacing: 15
  - Child Alignment: Middle Center
  - Child Force Expand: Width ✓, Height ✓
```

---

## 🎮 사용 방법

코드에서는 이미 자동으로 연동되어 있습니다:

```csharp
// BattleController.cs의 StartClashPhase()에서:
if (diceAnimationManager != null)
{
    diceAnimationManager.SetupDiceVisuals(playerPage, bossPage);
    yield return StartCoroutine(diceAnimationManager.AnimateClashSequence(
        player, playerPage, boss, bossPage));
}
```

애니메이션이 자동으로:
1. 주사위 비주얼 생성
2. 롤링 애니메이션 실행
3. 합 결과에 따른 승패 애니메이션
4. 데미지 적용
5. 일방 공격 처리
6. 페이드아웃 및 정리

---

## 🎨 커스터마이징

### 색상 변경:
`DiceVisual.cs`의 Inspector에서:
- Attack Color
- Defense Color

### 애니메이션 속도 조정:
`DiceVisual.cs`의 Inspector에서:
- Roll Duration (더 빠르게: 0.5, 더 느리게: 1.2)
- Rotation Speed (더 빠르게: 1080, 더 느리게: 360)

### 타이밍 조정:
`DiceAnimationManager.cs`의 Inspector에서:
- Delay Between Dice: 주사위 사이 간격
- Delay After Roll: 굴린 후 대기
- Delay After Clash: 합 후 대기

---

## 🔊 추천 사운드

필요한 효과음:
1. **Dice Roll Sound**: 주사위 굴리는 소리 (0.5~1초)
2. **Dice Result Sound**: 짧은 "띵!" 소리 (0.1~0.3초)
3. **Clash Sound**: 충돌 소리 (0.3~0.5초)
4. **Damage Sound**: 타격음 (0.2~0.4초)

Unity Asset Store에서 "Dice Roll" 또는 "Game UI" 검색 추천!

---

## 🐛 문제 해결

### 주사위가 안 보여요:
- DiceVisual 프리팹이 올바르게 설정되었는지 확인
- Canvas의 Render Mode 확인 (Screen Space - Overlay)
- Dice Container의 RectTransform 위치 확인

### 애니메이션이 너무 느려요/빨라요:
- DiceAnimationManager의 Delay 값들 조정
- DiceVisual의 Roll Duration 조정

### 색상이 안 바뀌어요:
- DiceVisual의 Dice Background가 Image 컴포넌트를 가지고 있는지 확인
- Inspector에서 Attack/Defense Color가 설정되어 있는지 확인

---

## 📝 추가 개선 아이디어

1. **파티클 효과**: 승리 시 반짝이는 파티클 추가
2. **트레일 효과**: 주사위 회전 시 잔상 효과
3. **카메라 쉐이크**: 큰 데미지 시 화면 흔들림
4. **슬로우 모션**: 결정적인 순간에 시간 늦추기
5. **데미지 텍스트**: 데미지 수치가 튀어오르는 효과

---

생성된 파일:
- `Assets/Scripts/BossBattle/DiceVisual.cs`
- `Assets/Scripts/BossBattle/DiceAnimationManager.cs`
- `Assets/Scripts/BossBattle/BattleController.cs` (수정됨)
